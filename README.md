# devspace

My editor and command line utilities distributed as a nix flake. This flake
does not produce any package outputs and is consumed as an attr-set output. All
it outputs is a `devspace.modules` attr set.
